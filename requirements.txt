# RevAIsor Access Review Agent — Dependencies
# Install with: pip install -r requirements.txt

# ── LLM SDKs (install whichever you plan to use) ──────────────────────────
openai>=1.0.0          # OpenAI GPT models
google-generativeai>=0.5.0  # Google Gemini models

# ── Optional: Knowledge Graph library ─────────────────────────────────────
# networkx>=3.0         # Uncomment if you prefer a graph library over dicts

# ── Optional: Config / data ───────────────────────────────────────────────
# pyyaml>=6.0           # Uncomment if you define your KG in a YAML file